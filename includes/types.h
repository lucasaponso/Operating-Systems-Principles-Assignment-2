#include <string>
#include <iostream>
#include <queue>
#include <fstream>
#include <vector>
#include <algorithm>
#include <map>
#include <sstream>
#pragma once
namespace osp2023 {
// renaming some types so that the types are more meaningful
using time_type = long long;
using id_type = int;
// constant that can be used to initialise a time to not having been set
constexpr time_type time_not_set = -1;
constexpr id_type id_not_set = -1;
}